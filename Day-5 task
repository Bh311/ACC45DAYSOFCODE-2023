#include <iostream>
using namespace std;

int main() {
	int T,D;	
	cin>>T;
	while(T>0)
	{
	    int flag=0;
	    cin>>D;
	    string N;
        cin>>N;
        for(int i=0;i<=D;i++)
        {
            if(N[i]=='0' || N[i]=='5')
            {
                flag=1;
            }
        }
     if(flag==0)
     {
         cout<<"No"<<endl;
     }
     else
     {
         cout<<"Yes"<<endl;
     }
	    T--;
	}
	return 0;
}
