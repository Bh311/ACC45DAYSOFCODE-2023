#include <iostream>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t>0)
	{
	    int A[5],count=0;
	    for(int i=0;i<5;i++)
	    {
	        cin>>A[i];
	        if(A[i]==1)
	        {
	            count++;
	        }
	        else
	        {
	            
	        }
	    }
	    if(count==0)
	    {
	        cout<<"Beginner"<<endl;
	    }
	     else if(count==1)
	    {
	        cout<<"Junior Developer"<<endl;
	    }
	      else if(count==2)
	    {
	        cout<<"Middle Developer"<<endl;
	    }
	    else if(count==3)
	    {
	        cout<<"Senior Developer"<<endl;
	    }
	    else if(count==4)
	    {
	        cout<<"Hacker"<<endl;
	    }
	    else
	    {
	        cout<<"Jeff Dean"<<endl;
	    }
	    t--;
	}
	return 0;
}
